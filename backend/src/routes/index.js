/*import express from 'express';
import { getProteins, getCrosslinks } from '../controllers/main.js';

const router = express.Router();
router.get('/proteins', getProteins);
router.get('/crosslinks', getCrosslinks);
export default router;
*/

